# -*- coding: utf-8 -*-
from setuptools import setup, find_packages

print(find_packages())

setup(name='code',
      packages=find_packages(),
      author_email='nomail@nodomain.com',
      license='INTERNAL',
      )
